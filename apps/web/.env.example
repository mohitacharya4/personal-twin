# Base URL of the Personal Twin API. Leave empty to use same-origin requests, which the
# Vite dev server proxies to http://localhost:8000 (see vite.config.ts) and the Docker
# image proxies via nginx. Set this only when the API is on a different origin.
VITE_API_BASE=
