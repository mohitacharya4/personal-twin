# Normalize line endings: text files use LF in the repo, checked out native on Windows.
* text=auto eol=lf

# Shell scripts must stay LF even on Windows (they run in Linux containers).
*.sh text eol=lf

# Binary assets should never be normalized.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
